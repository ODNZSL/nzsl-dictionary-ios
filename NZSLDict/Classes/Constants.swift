// These are constants defined in the old ObjC app

// TODO: there is probably a better way to handle these in swift

let EntrySelectedName = "EntrySelected"
let AppThemePrimaryColor = UIColor(named: "brand-primary")
let AppThemeAccentColor = UIColor(named: "brand-accent")
let AppSecondaryTextColour = UIColor(named: "text-secondary")
let AppThemePrimaryLightColor = UIColor(named: "brand-primary-light")
